import React from 'react'
import PropTypes from 'prop-types'
import cx from 'classnames'

import './style.scss'

const Logo =() => (
    <div
        {...{
            className: cx(
                'Logo'
            )
        }}
    >
        Logo
    </div>
)

export default Logo